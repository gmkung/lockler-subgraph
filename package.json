{
  "name": "lockler-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ your-username/lockler-subgraph",
    "create-local": "graph create --node http://localhost:8020/ your-username/lockler-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ your-username/lockler-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 your-username/lockler-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.97.0",
    "@graphprotocol/graph-ts": "^0.38.0"
  }
}
